/*import React from "react";
import RecipeListRandom from "../../components/RecipeList";

export default function RandomRecipesPage() {
  const { data } = useContext(DataContext);
  return (
    <>
      <RecipeListRandom recipes={data} />
    </>
  );
}
